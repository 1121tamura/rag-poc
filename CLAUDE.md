# CLAUDE.md

## 作業の進め方（最重要）

- ファイルやロジックをまとめて勝手に作らない。1つずつ内容を提示し、確認を取ってから作成する
- 「配線」（接続クライアントを返す関数など）と「実装ロジック」（アルゴリズム・変換処理など）は別物として扱う。ロジックを書く前は必ず確認を取る
- 仕様書（`doc/仕様書.md`）に明記されていない設計判断を独断で決めない。曖昧な点はユーザーに確認する
- 実装は勝手に大量に作らない。必ず１関数、１クラスなどのレベルでレビューして進めます。

## 作業範囲（絶対厳守）

- **プロジェクトディレクトリ（`/Users/tam/app/rag-poc`）の外は、読み取りも含めて一切触らない。** ホームディレクトリ配下（`~/.ssh`、`~/Downloads` など）の閲覧・検索・移動・削除はすべて禁止
- プロジェクト外での作業が必要な場合は、実行するコマンドを提示するだけにとどめ、実行はユーザーが行う
- `find ~` のようにホーム全体を走査するコマンドも対象に含む

## 回答前の状態確認（必須）

- **ファイルの有無・追跡状態・変更状態を断定する前に、必ずその場で確認する。** 数ターン前の調査結果や記憶を根拠にしない。ユーザーが並行して作業しているため、状態は常に変わっている前提で動く
- 特に `git status` / `git log` / ファイルの存在確認は、言及する直前に実行する
- 確認していないことを述べる場合は、推測であることを明示する

## 実装順序

API/UIを先に作らない。RAGのコア部分から実装し、動作確認してからAPI・UIを被せる。

1. Ingestion（まずMarkdown 1形式のみ）
2. 検索＋生成（RAGServiceの実ロジック）
3. FastAPI
4. Streamlit UI

## 技術的な決定事項

- パッケージ管理は uv。依存バージョンは `pyproject.toml` で `==` 固定する
- コンテナ内では `.venv` を作らない。`UV_PROJECT_ENVIRONMENT=/usr/local` を設定し、システムPythonに直接インストールする
- ハイブリッド検索（dense+sparse）はフェーズ1から採用（`doc/仕様書.md` 8章）。sparseベクトル取得には `FlagEmbedding` ライブラリ（`BGEM3FlagModel`）が必要
- LLM（Qwen3 8B）のダウンロード（`ollama pull qwen3:8b`）は、「検索＋生成」の実装に着手する直前に行う。Ingestion段階では不要

## 参照

- 機能仕様：`doc/仕様書.md`
