# 実装手順メモ

進め方のルールは `CLAUDE.md` 参照（1関数・1クラス単位でレビューしながら進める）。
このファイルは各ステップの作業内容・目的・状態を記録するメモ。実装が進むたびに更新する。

## 0. 開発環境（Devコンテナ）

`.venv`をホストに作らず、Devコンテナ自体を隔離された実行環境として使う方針（`CLAUDE.md`参照）。
コードを書いて動かす前に、実行できる環境を先に用意する。

| # | 内容 | 対象ファイル | 目的 | 状態 |
|---|---|---|---|---|
| 0-1 | Devコンテナ定義（試作） | `.devcontainer/devcontainer.json` | `image`+`features`（docker-in-docker）で試作 | 撤回（moby installでエラー） |
| 0-2 | 最小限のpyproject.toml | `pyproject.toml` | Devコンテナのビルド確認に必要な最小構成（依存関係はステップ1着手時に追加） | 完了 |
| 0-3 | Devコンテナ定義（features版） | `.devcontainer/devcontainer.json` | `features`をdocker-outside-of-dockerに変更 | 撤回（featuresが分かりにくいためcompose方式に変更） |
| 0-4 | Dockerfile | `Dockerfile` | devコンテナのベースイメージ（`python:3.11-slim`＋uv、`.venv`は作らずシステムPythonへ） | 完了 |
| 0-5 | docker-compose.yml | `docker-compose.yml` | `api`（Dockerfileからbuild）と`qdrant`（公式イメージ）の2サービスを定義。VS Codeは`api`にアタッチする | 完了 |
| 0-6 | Devコンテナ定義（compose版） | `.devcontainer/devcontainer.json` | `dockerComposeFile`+`service: api`を参照するだけのシンプルな形に変更。`features`は使わない | 完了（`docker compose build api`でビルド確認済み） |
| 0-7 | Ollamaサービス定義 | `docker-compose.yml` | Ollama（Qwen3 8B）はホスト実行ではなく別コンテナ（`ollama/ollama`公式イメージ）で起動する方針に決定。`api`からはサービス名`ollama`で接続可能。モデルのpull（`ollama pull qwen3:8b`）は「検索＋生成」実装着手時に行う | 完了（サービス定義のみ。pullは未実施） |

## 1. Ingestion（仕様書6章）

対象範囲：まずMarkdown 1形式のみで、Ingestionの一連の流れ（読み込み→チャンク分割→メタデータ付与→Embedding→Qdrant格納）を通す。CSV/Word/Excelは後回し。

| # | 内容 | 対象ファイル・関数 | 目的 | 状態 |
|---|---|---|---|---|
| 1 | Markdownの読み込み・チャンク分割 | `src/infrastructure/ingestion/loaders/markdown_loader.py` の `load_markdown()` | 仕様書6.1②。LlamaIndexの`MarkdownNodeParser`で見出し（章・節）単位にチャンク分割する | 完了 |
| 2 | メタデータ付与 | `markdown_loader.py` の `extract_chapter_title()` / `attach_metadata()` | 仕様書5章のメタデータスキーマ通りに、Qdrant格納前のチャンクへ情報を付与する。`document_type`/`file_format`/`updated_at`は呼び出し側（ingest_documents.py）から渡す想定、`chapter_title`のみ本文先頭行の見出しから自動抽出 | 完了 |
| 3 | Embedding生成（BGE-M3、dense） | `src/infrastructure/llama_index_factory.py` の `get_embed_model()` | チャンクをベクトル化する。まずdenseのみ実装し、sparse（ハイブリッド検索）は別ステップで追加する | 完了 |
| 4 | Qdrantへの格納 | `src/infrastructure/qdrant_vector_store.py` の `get_vector_store()` を使った格納処理 | 仕様書6.1⑤。生成したチャンク＋メタデータ＋ベクトルをQdrantに登録する | 完了（接続・コレクション取得の配線のみ。格納処理自体はステップ7で実装） |
| 5 | 差分判定 | `ingest_documents.py` の `_load_last_run_time` / `_iter_changed_files` | 仕様書6.2。ファイルのmtimeと前回実行時刻を比較し、変更ファイルのみを対象にする | 完了 |
| 6 | 重複防止（削除→再登録） | `ingest_documents.py` の `_delete_existing_chunks` | 仕様書6.3。対象ファイルの既存チャンクを`file_path`キーでQdrantから削除してから登録し直す | 完了（削除のみ。再登録はステップ7で実装） |
| 7 | 実行エントリポイント | `ingest_documents.py` の `run()` | 上記1〜6を1ファイルずつ処理する一連の流れとして繋ぐ | 完了（`_save_last_run_time`も追加。動作確認済み） |
| 8 | sparseベクトル生成（ハイブリッド検索） | `llama_index_factory.py` の `sparse_doc_vectors()` | 仕様書8章。`FlagEmbedding`（`BGEM3FlagModel`）でsparseベクトルを取得し、Qdrantのハイブリッド検索に対応する | 完了（`get_vector_store()`に`enable_hybrid`・`sparse_doc_fn`・`sparse_query_fn`を設定し`run()`に組み込み済み。dense+sparse両方の格納を確認。副次的に発見した初回実行時のコレクション未存在バグ（`_delete_existing_chunks`）も修正） |
| 9 | CSV/Word/Excelローダー | `csv_loader.py` / `docx_loader.py` / `excel_loader.py` | 仕様書6.4。Markdown以外の形式に対応する | 保留（TODO。Word/Excelはチャンク分割方式が未確定のため要設計判断。「検索＋生成」着手を優先） |

## 2. 検索＋生成（仕様書7章、RAGService）

対象範囲：RAGServiceの実ロジック（検索＋LLM生成）。API/UIより先にコア部分を実装し、動作確認してから被せる（`CLAUDE.md`参照）。

| # | 内容 | 対象ファイル・関数 | 目的 | 状態 |
|---|---|---|---|---|
| - | Qwen3 8Bのpull | Ollama HTTP API（`/api/pull`） | このフェーズ着手時に実施する方針（`CLAUDE.md`参照）。`api`コンテナに`docker`/`ollama`CLIが無いため、`docker exec`ではなくHTTP API経由で実施 | 完了（`qwen3:8b`、5.2GB、`ollama_data`ボリュームに永続化済み） |
| 1 | 検索（Qdrantへのハイブリッドクエリ） | `src/application/rag_service.py` の `search()` | 仕様書7.1③・8章。質問文でQdrantをハイブリッド検索し、`document_type`によるメタデータフィルタリングにも対応。`TOP_K=5`（人手レビューで調整する前提の初期値） | 完了（サンプルデータで関連度・フィルタとも動作確認済み） |
| 2 | Ollama連携（LLM接続） | `src/infrastructure/ollama_client.py` の `get_llm()` | 仕様書7.1④。Qwen3 8BへのLlamaIndex経由の接続。`request_timeout=300秒`（CPU推論を考慮） | 完了（動作確認済み。開発環境のDockerメモリ不足（6.2GiB→16GBへ増設）が判明・対応） |
| 3 | 回答生成（ハルシネーション対策） | `rag_service.py` の `generate_answer()` | 仕様書7.2〜7.3。検索結果をコンテキストにOllamaで`astream_chat`によるストリーミング回答生成。システムプロンプトで参照文書外は推測させず、ソース（`nodes`）も返す | 完了（文書内・文書外の両質問で動作確認済み） |
| 5 | 類似度閾値チェック（対策3） | `qdrant_vector_store.py` の `hybrid_fusion_with_raw_scores()`、`rag_service.py` の `has_relevant_document()` | 仕様書7.3対策3。**融合後スコアは結果セット内の相対値**（既定の`relative_score_fusion`が0〜1に正規化して合成するため、無関係な質問でも1位が必ず0.5以上になる）で閾値判定に使えないため、カスタム融合関数で融合前の生スコアをノードのメタデータに保持し判定する。検索は1回のまま。判定は **dense ≥ 0.55 または sparse ≥ 0.12**（OR）。閾値未満なら参照文書を渡さず、`NO_DOCUMENT_PROMPT`で言い換えを案内する。「分かりません」の一言で終わらせない方針に伴い`SYSTEM_PROMPT`も改訂（仕様書7.3の確定文言も更新済み） | 完了（15問すべて期待通りの判定。関連あり/無関係/挨拶/文書内だが記載なしの4パターンで実機の応答も確認） |
| 4 | Embeddingモデルのキャッシュ | `llama_index_factory.py` の `get_embed_model()` / `_get_sparse_model()` | 検索1回ごとにBGE-M3（約2.2GB）を読み込み直していたため、`lru_cache`でプロセス内1回に変更。1検索あたり約0.3秒になった（修正前は15問の測定が120秒超）。トレードオフとしてdense用・sparse用の2モデル（計約4.5GB）が常駐する。`BGEM3FlagModel`は本来1モデルでdense/sparse両方を出力できるため統合すればメモリ半減できるが、LlamaIndexのdense側が`HuggingFaceEmbedding`前提の配線のためカスタムEmbeddingクラスが必要。今回は見送り | 完了（同一インスタンスが返ること・所要時間を確認） |

## 3. FastAPI（仕様書11章プロジェクト構成）

| # | 内容 | 対象ファイル・関数 | 目的 | 状態 |
|---|---|---|---|---|
| 1 | `/query`エンドポイント | `src/interfaces/api/query_router.py` | 仕様書7.1②・7.2。`generate_answer()`をHTTP経由で呼び出し、NDJSON（sources→token）でストリーミング返却する。ロジックは持たず翻訳のみ（interfaces層） | 完了 |
| 2 | FastAPIアプリ本体 | `src/main.py` | `query_router`を登録するエントリポイント | 完了 |

## 4. Streamlit UI（仕様書11章プロジェクト構成）

| # | 内容 | 対象ファイル | 目的 | 状態 |
|---|---|---|---|---|
| 1 | チャットUI・データ分析タブ枠 | `ui/app.py` | 仕様書7.1①⑥⑦・12.4。`st.chat_input`/`st.write_stream`でチャット表示、`document_type`絞り込み、ソース表示。「データ分析」タブはフェーズ2向けプレースホルダーのみ | 完了（FastAPI経由のHTTP呼び出しロジックはend-to-end確認済み。ブラウザでの目視確認は未実施） |
| 2 | VSCodeからの一括起動 | `.vscode/launch.json` | FastAPI・Streamlitを「実行とデバッグ」パネルから同時起動できるようにする（compound構成）。Qdrant/Ollamaはdevcontainer起動時にdocker-composeで自動起動するため対象外 | 完了。`debugpy`は`pyproject.toml`の`[dependency-groups] dev`に追加し、`uv sync`で自動的に入るようにした（devグループはuvのデフォルトで含まれる） |
| 3 | 挨拶・世間話への対応 | `rag_service.py` の `SYSTEM_PROMPT` | ブラウザ実機テストで発覚：`search()`が関連度に関わらず常に上位5件を返すため、「こんにちは」等の非質問入力でも無関係なチャンクが渡り、厳格なシステムプロンプトにより「分かりません」と返答される問題。プロンプトに「挨拶や世間話には自然に応答してよい」旨を追記して緩和 | 完了（挨拶・文書内質問・文書外事実質問の3パターンで回帰確認済み） |
| 4 | ローディング表示 | `ui/app.py` | 検索中（ソース確定まで）を`st.spinner`で表示し、無反応に見える待ち時間を解消 | 完了（ロジックは確認済み。ブラウザでの目視確認はユーザー側で実施） |

## 5. 会話履歴・マルチターン対話（仕様書7.4）

永続化層（`sqlite_conversation_repository.py`）と `trim_history()` は先行して実装済みだったが、
どこからも呼ばれていない状態だったため、配線とロジックを順に実装する。

| # | 内容 | 対象ファイル・関数 | 目的 | 状態 |
|---|---|---|---|---|
| 1 | 利用者識別 | `src/interfaces/api/dependencies.py` の `get_current_user_id()` | 仕様書7.4。`X-User-Id`ヘッダを読み `local:<名前>` を返す。利用者の識別方法を知る唯一の関数とし、認証追加時はここだけを差し替える。ヘッダ未指定はFastAPIの検証エラーで422、空白のみは400 | 完了（動作確認済み） |
| 2 | 会話CRUDエンドポイント | `src/interfaces/api/conversation_router.py` | 仕様書7.4。一覧/作成/メッセージ取得/削除の4本。ロジックは持たず永続化層へ委譲する翻訳のみ。タイトルは仮値`新しい会話`で作成し、初回質問時にLLM生成の値へ差し替える方式を採用（会話作成をLLM待ちでブロックしないため） | 完了（所有者以外は参照・削除不可を含め動作確認済み） |
| 3 | タイトル更新 | `sqlite_conversation_repository.py` の `update_title()` | ステップ2の仮タイトルを初回質問時に差し替えるために必要。`updated_at`は更新しない（同時に呼ばれる`add_message()`が更新するため） | 完了（動作確認済み） |
| 4 | 履歴を含めた回答生成 | `rag_service.py` の `generate_answer()` | 仕様書7.4。直近5往復を`trim_history()`で予算内に切り詰めてプロンプトに含める。履歴の読み出しは`rag_service`が永続化層から直接行い、`query_router`は翻訳のみに保つ。戻り値は要素が増えるため`AnswerResult`データクラスに変更 | 完了（直近5往復のみ含まれること・初回は履歴なしで2件のみ・予算超過時に往復単位で切り詰めが起きることを確認） |
| 5 | query rewriting | `rag_service.py` の `rewrite_query()` | 仕様書7.4。履歴がある場合のみ質問を単独で意味の通る検索クエリに書き換える。解決できない指示語は推測で補完せず原文のまま返す。書き換えは検索用に留め、プロンプトへ渡す質問は原文のまま（履歴も同梱されるため文脈が通る）。案内表示の要否はUI側が判断できるよう、サービス層は`is_query_rewritten`という事実のみ返す | 完了（解決できる指示語は書き換え・解決できない指示語は原文のまま・初回は書き換えLLM呼び出し0回を確認） |
| 6 | 会話タイトルのLLM生成 | `rag_service.py` の `generate_title()` | 仕様書7.4。初回質問からタイトルを生成する。呼び出しは回答のストリーム完了後（CPU推論では追加のLLM呼び出しが初回応答を遅らせるため）。初回かどうかの判定は`AnswerResult.is_first_question`で返し、追加のDBアクセスを避ける。LLMが空や長文を返した場合は20文字で切り詰め・質問文にフォールバック | 完了（`is_first_question`の3パターンと実機でのタイトル生成を確認） |
| 7 | `/query`への`conversation_id`追加 | `query_router.py`、`rag_service.py`の`save_exchange()`、`sqlite_conversation_repository.py`の`owns_conversation()` | 仕様書7.4。会話IDを受け取り履歴の読み出し・保存・通知を繋ぐ。NDJSONの末尾に`done`行（`is_history_trimmed`/`is_query_rewritten`/`title`）を追加。保存はストリーム完了後（質問を先に保存すると自身が履歴として読み出されるため。途中切断時はその往復が保存されない点は許容）。永続化層へのアクセスは`rag_service`に閉じ、ルーターは翻訳のみに保つ。他人の`conversation_id`への書き込みは404で拒否 | 完了（初回/2回目/他人のID/会話IDなしの4パターンを確認） |
| 8 | UI（会話一覧・利用者名） | `ui/app.py` | 仕様書7.4。サイドバーに利用者名入力と会話一覧（切替・削除）、過去会話の再表示（sources含む）、`done`行による切り詰め通知・指示語の案内・タイトル反映。会話の作成は最初の質問時（空の会話を一覧に残さない）。表示は会話切替時にサーバから読み込み、送信中の往復はローカルに追記。利用者名が未入力の間は入力欄を無効化。指示語の案内は「書き換えなし かつ 回答に『分かりません』を含む」場合のみ（自己完結した質問でも書き換えなしになるため） | 完了（APIはend-to-endで確認済み。ブラウザでの目視確認は未実施） |

## 次の一手

ステップ5（会話履歴・マルチターン対話）は5-1〜5-8まで完了。実機のLLMを使ったend-to-end確認で、
初回質問→タイトル生成、追加質問「それは何段階？」の書き換え（`is_query_rewritten: true`）、
会話の保存（sources含む）、削除まで動作を確認済み。ブラウザでの目視確認は未実施。

ハルシネーション対策（仕様書7.3）は対策1〜3すべて実装完了。閾値の決定にあたり、
開発用サンプル文書を`docs/`配下に6本作成して取り込んだ（45チャンク）。
取り込み済みだった`/tmp`由来のゴミチャンク1件は削除済み。

## 6. デプロイ準備

| # | 内容 | 対象ファイル | 目的 | 状態 |
|---|---|---|---|---|
| 1 | 本番用Compose設定 | `docker-compose.prod.yml`（新規）、`docker-compose.gpu.yml`（新規） | 開発用の`docker-compose.yml`はapiを`sleep infinity`で待機させる構成のため本番起動できず、Streamlitのサービス定義も無かった。CPU前提の基本ファイルと、GPU割り当てを重ねる上書きファイルの2本立てにした（Composeの標準的な構成）。公開ポートは8501のみ | 完了（YAML構文とサービス構成を確認） |
| 2 | コードのイメージ焼き込み | `Dockerfile` | 本番はホストのマウントに頼れないため`src/`・`ui/`・`.streamlit/`をCOPYする。依存を先にインストールしてレイヤキャッシュを効かせる。開発ではホストの`.`が`/workspace`にマウントされ上書きされるため影響しない | 完了 |
| 3 | デバイス切り替え | `llama_index_factory.py` | 環境変数`EMBEDDING_DEVICE`でCPU/GPUを切り替える（既定`cpu`）。`use_fp16`はGPUでのみ有効なためデバイスに連動させた | 完了（既定cpu・cuda指定時の切り替えを確認。CPUでの検索動作も回帰確認済み） |
| 4 | 取り込み状態ファイルの外出し | `ingest_documents.py` | 本番はコードをイメージに焼き込むため、`INGESTION_STATE_FILE`でボリューム側を指定できるようにした（既定は従来通りルート直下） | 完了（既定・環境変数指定の両方を確認） |
| 5 | GPU用PyTorchの切り替え箇所 | `pyproject.toml` | CUDA版PyTorchは2GB超あり、CPUのDevコンテナに入れると開発環境が重くなるため**適用はしていない**。CPU版を既定のまま残し、GPU環境で入れ替えるCUDA版の指定をコメントで併記した。差し替え後は`uv lock`が必要 | コメント併記のみ（意図的に未適用） |
| 6 | 手順書 | `doc/デプロイ手順_AWS_EC2_GPU.md`、`doc/デプロイ手順_オンプレミス.md`、`doc/ドキュメント再取り込み手順.md` | RAG実装初心者向けの具体的な手順。READMEからリンク。金額表記は円建て（1米ドル150円換算の旨を注記） | 完了（再取り込み手順は実機で検証済み：コレクション削除→状態ファイル削除→再取り込み42.6秒→45チャンク復元） |
| 7 | 仕様書の最新化 | `doc/仕様書.md` | 実装・決定済みだが未反映だった6か所を反映：7.2（`done`行の追加）、7.4（実装時に確定した事項5点）、6.2（状態ファイルの環境変数化）、8章（融合方式とモデル常駐）、10.2（環境ごとのCompose構成）、11章（プロジェクト構成）、12章（データ分析タブを用意しない決定） | 完了 |

## 次の一手（更新）

残りのTODO：
- CSV/Word/Excelローダー（1-9。Word/Excelはチャンク分割方式が未確定のため設計判断が必要）
- 閾値（dense 0.55 / sparse 0.12）は開発用サンプル文書での暫定値。実文書の投入後に人手レビューで見直す
- 回答の断定が強く出るケースの観察（サンプル文書に明記のない内容を「明記されています」と述べた例あり）
- ~~データ分析タブの枠~~（仕様書12章。**タブは用意しない方針に決定**。フェーズ1は文書RAGのみとする。
  理由：フェーズ2の実装時にどのみちUI構造を見直すこと、および`st.chat_input`はタブ内に置くと
  画面下部への自動固定が効かず、枠を置くだけでも現状のUI構造に手を入れる必要があるため）
- ブラウザでの目視確認（4-4、5-8）
